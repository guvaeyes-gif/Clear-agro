# Config

Templates de configuracao por ambiente e modulo.

Subpastas:
- `env/`: variaveis e exemplos de ambiente.
- `paths/`: caminhos canonicos e de compatibilidade por dominio.
- `templates/`: modelos de status, log e auditoria.

