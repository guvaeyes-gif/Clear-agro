# Integrations / Shared

Componentes reutilizaveis de integracao (auth, utilitarios, contratos).

- `lock_utils.py`: lock operacional por recurso com trilha em `logs/audit`.
- `bling_paths.py`: resolucao centralizada da raiz canonica e de compatibilidade dos caches Bling.

